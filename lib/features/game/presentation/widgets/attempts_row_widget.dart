import 'package:flutter/material.dart';

class AttemptsRowWidget extends StatelessWidget {
  const AttemptsRowWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
